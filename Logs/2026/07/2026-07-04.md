# Logging for 2026-07-04

> Token counts and CO2 use the conventions in `Logs/logging.md`
> (0.076 gCO2 per 1,000 tokens). Authoritative per-prompt numbers are in
> `Logs/log_summary.csv`.

---

## Entry 1

- **UT time:** 2026-07-04T11:38:43Z (prompt received)
- **Prompt:** "Re-Read the context.md prompt file in Projects/ClimateIntelligence/claude_prompts/  Execute the 1st prompt."
- **Model:** claude-fable-5; authoritative value in `log_summary.csv`
- **Tokens used:** estimate; authoritative value in `log_summary.csv`.
- **CO2 estimate:** see `log_summary.csv` (authoritative).

**Work done / thinking:**
- Read the new `claude_prompts/context.md`; prompt 1: grab every major IPCC
  climate report, stage in `context/Reports/`, copy to the ClimateIntelligence
  shared Google Drive with `rclone`, ask questions in Discussion, log the work.
- **Scope decision:** interpreted "every major report" as the complete AR6
  assessment cycle (current; AR7 unpublished): WGI, WGII, WGIII full reports,
  the Synthesis Report, and the three Special Reports (SR1.5, SROCC, SRCCL) —
  7 PDFs, ~965 MB. Posted 4 questions to the Discussion section (earlier
  cycles? standalone SPMs? Drive layout? gitignore choice).
- Found an `rclone` remote named `ClimateIntelligence:` already configured
  (Google shared/team drive, empty). Destination: `ClimateIntelligence:Reports/`.
- Verified all 7 download URLs with HEAD requests first; two initial guesses
  (report.ipcc.ch) 404'd and were corrected to www.ipcc.ch paths.
- Downloaded in a background job. **Mistake caught:** the WGI report (404 MB,
  the largest) hit the job's 550 s per-file timeout and was truncated at
  266 MB — its missing "done" line and the size mismatch gave it away.
- Wrote `bin/verify_pdf_reports.py` (per the Calculations rule and Coding
  guidelines) checking %PDF magic, trailing %%EOF, and (when pypdf exists)
  page count. Result: 6 OK, WGI correctly flagged BAD.
- Resumed WGI with `curl -C -`; first attempt died with exit 92 (HTTP/2
  framing error on the range request), retried over HTTP/1.1.
- Uploaded the six verified PDFs with `rclone copy` while WGI recovered:
  602 MiB transferred at ~4.2 MiB/s; byte counts on the Drive match local.
- Added `context/Reports/` to `.gitignore` (~1 GB of PDFs; the Drive is their
  home — flagged in Discussion in case git-tracking is preferred).
- WGI completion, its verification, and its upload are recorded below.

**WGI outcome:** (in progress at time of writing; see addendum)
